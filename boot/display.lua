local computer = computer
local component = component

ScreenList = {}
GPUList = {}

do
    
end

Display =
{
    gpu = component.list("gpu")(),
    screen = component.list("screen")(),
    terminal =
    {
        cy = 1,
    }
}
